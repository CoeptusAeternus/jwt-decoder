services:
  app:
    build:
      context: .
      target: development
    ports:
      - "9000:9000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: pnpm dev -- --host 0.0.0.0 --port 9000 --no-open

volumes:
  node_modules: